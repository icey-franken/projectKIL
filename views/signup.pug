extends layout.pug

include include/auth-input.pug

append head
	script(src='/public/js/signup.js' type='module' defer)

block content
	div(class="auth-container")
		form(class="form-signin" id="signup-form")
			div(class="text-center mb-4 ")
				ul#errors-container
				+authButtons()
				+authInput("Email","email")
					input(class="form-control" type='email' name='email' placeholder='Email' required)
				+authInput("Username","username")
					input(class="form-control" type='text' name='username' placeholder='Username' required)
				+authInput("Password","password")
					input(class="form-control" type='password' name='password' placeholder='Password'  required)
				+authInput("Confirm Password","password2")
					input(class='form-control' type='password' name='password2' placeholder='Confirm Password'  required)
				.row
					div(class="col-md-5 mb-3")
						select(class="custom-select d-block w-100" name='aboutYouId')
							option(value=null) About You
							each aboutYou in aboutYous
								option(name='aboutYouId' value=aboutYou.id)= aboutYou.description
					div(class="col-md-5 mb-3")
						select(class="custom-select d-block w-100" name='countryId')
							option(value=null) Country
							each country in countries
								option(value=country.id) #{country.name} (#{country.code})
				button(class="btn btn-lg btn-primary btn-block" type='submit') Sign Up
